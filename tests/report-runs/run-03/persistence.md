# Persistence — Run 03

判定は`unknown`。

| 種別 | 内容 |
|---|---|
| `fact` | `list_imports`とmainの限定確認に永続化固有Evidenceなし |
| `inference` | 現在のEvidenceは永続化を支持しない |
| `unknown` | Run Key、Service、Task、WMI等（U-002） |

静的解析のCoverageが不足しており、不存在や成功は断定しない。
